"use strict"; //treat all js code as newer version
//alert(3 + 3); //in node it will not work but in browser it will work
// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined =>
// symbol => unique

// object
//suppose we are declaring the variable but not assiging the value it will be considered as a undefined
//null means its an empty value. If we check the type of null in console it will show us as an object

console.log(typeof null);